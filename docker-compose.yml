services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: flowplan
      POSTGRES_PASSWORD: flowplan
      POSTGRES_DB: flowplan
    ports:
      - '5432:5432'
    volumes:
      - flowplan_pg:/var/lib/postgresql/data

volumes:
  flowplan_pg:
