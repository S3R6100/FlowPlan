# Frontend — alcance actual

## Eliminado

- Selector de zonas / `GET /zones`
- `zoneId` en `GET /schedule`

## Horario

- Eventos **fixed**: borde sólido, se pueden cancelar.
- Eventos **dynamic**: borde punteado rosa, vienen del podómetro.
- Recomendación sin ubicación; al completar timer → `POST /sessions` con `startHour`, `endHour`, `title` de la recomendación.

## Tabs

- Horario · Tareas · Semana (sin cambios de rutas)

## Demo

`demo@flowplan.app` / `demo1234`
